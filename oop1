class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

print("Enter Name and Age:")
name=input()
age=int(input())


p1 = Person(name,age)

print(p1.name)
print(p1.age)
